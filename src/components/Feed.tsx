import Story from './Stories';

type Props = {};
const Feed = (props: Props) => {
  return (
    <main>
      <section>
        <Story />
      </section>
      <section></section>
    </main>
  );
};
export default Feed;
