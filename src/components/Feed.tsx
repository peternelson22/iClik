import Stories from './Stories';

type Props = {};
const Feed = (props: Props) => {
  return (
    <main>
      <section>
        <Stories />
      </section>
      <section></section>
    </main>
  );
};
export default Feed;
